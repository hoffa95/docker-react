sudo: required
services: 
  - docker

before_install:
  - docker build -t hofferpet/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true hofferpet/docker-react npm run test -- --coverage
